<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:tx="http://www.springframework.org/schema/tx"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd
		http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-4.3.xsd">

	<!-- DispatcherServlet Context: defines this servlet's request-processing infrastructure -->
	
	<!-- Enables the Spring MVC @Controller programming model -->
	<annotation-driven />

	<!-- Handles HTTP GET requests for /resources/** by efficiently serving up static resources in the ${webappRoot}/resources directory -->
	<resources mapping="/resources/**" location="/resources/" />

	<!-- Resolves views selected for rendering by @Controllers to .jsp resources in the /WEB-INF/views directory -->
	<!-- 뷰 리졸버 : 스프링에서 JSP주소를 자동완성 시켜주는 도구 -->
	<beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<beans:property name="prefix" value="/WEB-INF/views/" />
		<beans:property name="suffix" value=".jsp" />
	</beans:bean>
	
	<!-- 도구(컨트롤러, 서비스, repository등)를 탐색하는 기준 패키지 -->
	<!-- 
		serlvet에서 보듯이 요청과 관련된 객체를 정의한다
		url과 관련된 controller나 @(어노테이션),ViewResolver, Interceptor, MultipartResolver등등
		DispatcherServlet과 관련된 설정
		2. root-context
		서블릿과 반대로 view와 관련되지 않은 객체를 정의한다.
		따라서 service, Repository, DB등 비지니스 로직과 관련 정의
		3. web.xml
		설정을 위한 설정파일
		즉, 최초로 was가 최초로 구동될 때 가종 설정
		xml파일을 인식하도록 각 파일을 가리켜 줍니다.
	
		문제점 : 모든 annotion 등록이 serlvet-context.xml에 된다는 것
		- 원하는 파일로 자동으로 나눠서 등록되도록 구분할 필요가 있다.
		- @Controller : servlet-context.xml에 등록(요청 처리)
		- @Service : root-context.xml에 등록 요청과 무관하게 실행이 가능해야 한다.
		- @Repository : root-context.xml
		- @Component : root-context
		- use-default-filters: 기본적으로 제공되는 탐지옵션을 false하겠다. :: 직접 설정하겠다.
	 -->
	<context:component-scan base-package="com.kh.spring14" use-default-filters="false">
	<!-- com.kh.spring14는 불러오고 그 중에서 service, repositorry, component만 제외 -->
		<context:include-filter type="annotation" expression="org.springframework.stereotype.Controller"/>
	</context:component-scan>
	

	<!--  
		파일 업로드 도구 
		- 주의 : 반드시 id는 multipartResolver(DispatcherServlet의 요구사항)
		- org.springframework.web.multipart.commons.CommonsMultipartResolver
		옵션 :
			-defaultEncoding : 기본 처리 인코딩 방식
			- maxUploadSize : 총 업로드 크기 제한(byte)
			- maxUploadSizePerSize : 파일 1개당 업로드 크기 제한(byte)
	-->
	<beans:bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
		<beans:property name="defaultEncoding" value="UTF-8"/>
		<beans:property name="maxUploadSize" value="10000000"/>
		<beans:property name="maxUploadSizePerFile" value="10000000"/>
	</beans:bean>
	
	<!-- 
	 트랜잭션 도구 생성 및 설정
	 1. 트랜잭션 관련 설정 활성화(스프링 기능)(tx namespace 설정 후 작성)
	 	- @Transactional 이라는 설정이 정상적으로 작동함
	 2. 트랜잭션 관리 도구 등록
	 	- org.springframework.jdbc.datasource.DataSourceTransactionManager
	 	- 데이터베이스 연결 도구가 주입되어야 함 
	  -->
	<tx:annotation-driven/>
	<beans:bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
		<beans:property name="dataSource" ref="dataSource2"/>
	</beans:bean>
</beans:beans>
